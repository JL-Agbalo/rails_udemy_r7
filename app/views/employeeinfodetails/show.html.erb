<div class="container">
  <div class="row">
    <div class="col-md-2"></div>
    <div class="col-md-8 mt-4">
      <table class="table table-bordered mt-2">
        <thead>
          <tr>
            <th>First Name</th>
            <th>Last Name</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><%= @employee.firstname %></td>
            <td><%= @employee.lastname %></td>
          </tr>
        </tbody>
      </table>
      <%= link_to 'Back to Employee Path', employeeinfodetails_path, class: 'btn
      btn-secondary mt-3' %>
    </div>
  </div>
</div>

<!-- Certificates, CV and Photo are not working in uploading -->
